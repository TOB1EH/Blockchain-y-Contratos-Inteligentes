# Comunicación con un nodo utilizando JSON RPC

Estos *scripts* dialogan con un nodo intercambiando mensajes JSON vía HTTP o IPC. La única biblioteca utilizada es la necesaria para realizar las conexiones.